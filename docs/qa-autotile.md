# 오토타일 규칙 검증 — 2026-09-13

- 레이어별 점유 상태로 3×3 규칙을 평가하고 첫 일치 규칙 또는 기본 타일을 고른다.
- 원래 배치 ID/assetId를 수정하지 않으므로 설정 해제 시 원래 아트로 돌아간다.
- 화면과 PNG export가 같은 resolved placements를 사용한다.
- 규칙 schema, 참조 누락, 정확한 타일 크기, import 후 실제 PNG 크기 검증.
- portable project의 기본/규칙 타일 ID를 재할당된 자산 ID로 함께 변환한다.
- 단위 검사: 상단 경계, 이웃 삭제, 다른 레이어 보존, 규칙 순서, 설정 해제,
  잘못된 패턴/중복 ID/타일 크기/참조 누락 및 복원.

브라우저 localhost 테스트 공간에서 32px 단색 fixture로 기본/상단 가장자리 규칙을 만들었다.
맨 위 왼쪽 타일 삭제 후 바로 아래 셀이 가장자리 타일로 바뀌었다.
새 탭에서도 설정이 유지됐으며, 내려받은 768×512 PNG를 검사해 삭제 칸의 투명도,
이웃 가장자리·상단 가장자리·내부 타일 RGBA가 예상값과 정확히 일치했다.
편집 중 발생한 HMR 문법 오류는 수정했다. 최종 빌드와 새 탭에서는 관련 오류가 없었다.

가중치 랜덤 variant, 외부 업체 preset 형식 호환, AI 지형 타일 제작은 이번 범위 밖이다.
